// some todos os valores do array e imprima o resultado.

let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];
let = result = 0; // para somar os valores preciso criar uma variavel inicial.

for (let soma = 0; soma < numbers.length ; soma += 1) {
    result += numbers[soma];
}

console.log(result);